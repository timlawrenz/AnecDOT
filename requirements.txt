# Core dependencies for AnecDOT dataset generation

# Web scraping
beautifulsoup4>=4.12.0
lxml>=4.9.0
requests>=2.31.0

# TUI interface
textual>=0.47.0

# FSM library support (for parsing)
pydot>=2.0.0
graphviz>=0.20.0

# Synthetic generation (LLM providers)
google-generativeai>=0.3.0  # For Gemini API

# QLoRA training infrastructure
transformers>=4.36.0
peft>=0.7.0
bitsandbytes>=0.41.0
accelerate>=0.25.0
datasets>=2.16.0
torch>=2.1.0
sentencepiece>=0.1.99  # For some tokenizers
scipy>=1.11.0  # For training metrics

# Development and testing
pytest>=7.4.0
pytest-cov>=4.1.0
